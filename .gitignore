# Environment
.env
.env.local
*.env

# Service account JSON keys (NEVER commit)
service-account-*.json
*-credentials.json

# Python
__pycache__/
*.py[cod]
*$py.class
.venv/
venv/
.pytest_cache/
*.egg-info/

# OMC plugin / Claude tooling (per-user, repo X)
.omc/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# 사장님 임시 파일 (스크린샷 등)
docs/사장님-가이드/screenshots/

# 사장님 비즈니스 데이터 (public repo 노출 금지, 2026-05-11)
# comparison json = 사장님 시트 500행 키워드/링크/순위 비교 데이터
.harness/comparison-*.json

# 백업 파일 (= 사장님 시트 전체 데이터 = 비즈니스 정보, 2026-05-18)
# .harness/backups/ = run_cycle 시작 시 시트 K/L/M/링크 전체 snapshot
.harness/backups/

# H1 시계열 분석 출력 (= 사장님 시트 시계열 = 비즈니스 정보, 2026-05-18)
.harness/timeline_report*.csv

# Diagnostic artifacts may include sheet-derived row data.
.harness/audits/
.harness/traces/
.harness/type-previews/
.harness/stale-previews/
# 로컬 진단/백업 덤프(키워드·링크 포함). 공개 repo 유출 방지 — 절대 커밋 금지.
.harness/tmp/

# cron 사이클 요약(시트ID·집계 k_distribution 포함). CI 러너 임시파일이나 로컬 실행 시
# 워킹트리에 생겨 실수 커밋 위험 → 추적 제외 (security-reviewer 2026-07-02).
cycle_summary.json
