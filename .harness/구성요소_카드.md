# 구성요소 카드 — naver-rank-checker(상위노출 순위체커) 세부 시스템 전수
⚠️Claude 정리·사장님 컨펌 대기(2026-07-18)

> 조사 근거: `.github/workflows/` 9개 전수 + `src/`·`scripts/` 주요 스크립트 헤더 Read + `gh workflow list --all`·`gh run list` 라이브 실측(2026-07-18) + `.harness/요청_원장.md`(목적 앵커).
> GitHub의 workflow "active"는 '파일이 등록됨'일 뿐, 자동 실행 여부는 cron 유무로 갈림 — 아래 현황이 그 구분.

---

## A. 라이브 자동 (cron/트리거로 스스로 도는 것)

### [rank-check 본체 — naver-rank-check.yml + src/main.py]
⚠️Claude 정리·사장님 컨펌 대기(2026-07-18)
- 목적: "OO 카외" 탭의 키워드×링크를 네이버 통합검색에서 자동 체크해 K(노출영역)/L/M(순위)/O(지식인탭)를 시트에 자동 갱신 — 외주 33만원 도구 대체.
- 현황: 라이브 자동. cron 하루 4회(KST 00/06/12/18시, 각 07분·27분 이중 시도 + concurrency로 중복 차단), ubuntu-latest, 1사이클 약 54분~. 2026-07-18 run 성공 실측.
- 확인법: `gh run list --workflow=naver-rank-check` 최근 run success + 텔레그램 한 줄 알림 수신.

### [K열 상태전이·삭제 감지 — src/transitions.py]
⚠️Claude 정리·사장님 컨펌 대기(2026-07-18)
- 목적: 이전 vs 현재 비교로 K값 자동 결정(노출/미노출/누락/삭제) — 노출중지 자동 감지(K="삭제")가 외주본에 없던 차별점. K에 "M/D HH:MM~" 상태 시작 시점 포함(D-030).
- 현황: 라이브(rank-check 본체 내부 코어, 매 cron). 사장님이 K에 직접 쓴 단어는 보존.
- 확인법: 시트 K열이 cron 후 갱신되는지 + "삭제"/"누락" 표기가 실제 상태와 맞는지 육안 대조.

### [자가복구(내부 재시도) — rank-check.yml run step]
⚠️Claude 정리·사장님 컨펌 대기(2026-07-18)
- 목적: 일시적 네트워크/차단/구글 장애를 사람 개입 없이 복구(1차 실패 시 90초 후 1회 재실행). rc=2(코드/사이트 변경 의심)는 재시도 생략(결정적 실패, 2026-06-30 fix).
- 현황: 라이브(매 run 내장). 코드 버그 '자동 수정'은 안 함(라이브 시트 손상 위험).
- 확인법: 실패 run 로그에서 "[SELF-HEAL]" 문자열 검색.

### [watchdog — rank-check-watchdog.yml + scripts/notify_run_failure.py]
⚠️Claude 정리·사장님 컨펌 대기(2026-07-18)
- 목적: rank-check 실패 시 1회 자동 rerun(같은 코드 재실행만) + 텔레그램 알림. 연속실패 수 따라 알림 심각도 에스컬레이션(3연속 🟠, 5연속+ 🔴 — '9일 침묵' 방지).
- 현황: 라이브. workflow_run(failure) 트리거 — 성공 시엔 skipped로 지나감(정상). 토큰=github.token(옛 ACTIONS_PAT 401 사망 교훈).
- 확인법: `gh run list --workflow=naver-rank-check-watchdog` — 본체 성공 시 skipped, 실패 시 실행+텔레그램.

### [telegram-qa 양방향 Q&A봇 — telegram-qa.yml + scripts/telegram_qa_bot.py]
⚠️Claude 정리·사장님 컨펌 대기(2026-07-18)
- 목적: 사장님이 텔레그램(@sangno_bot)으로 물으면 몇 초 안에 답 — long-poll 루프(한 run 4h) + cron */5분이 다음 run을 큐에 대기시켜 끊김 없는 핸드오프. 자연어는 Groq 무료 LLM(미설정 시 키워드 매칭 폴백).
- 현황: 라이브(2026-07-18 in_progress run 실측. 핸드오프 구조상 completed/cancelled 섞이는 게 정상). 사장님 chat_id만 응답. ⚠️순위는 현재 스냅샷만(과거 월평균 질문 불가).
- 확인법: 텔레그램에 질문 → 몇 초 내 답장 오는지. + `gh run list --workflow=telegram-qa`에 in_progress 상시 존재.

### [즉시 보고(텔레그램 한 줄) — scripts/send_telegram_summary.py]
⚠️Claude 정리·사장님 컨펌 대기(2026-07-18)
- 목적: 매 cron 종료 직후 '돌았나/성공률' 딱 한 줄을 사장님 텔레그램으로(2026-06-20 피드백: 개발자 외계어 제거, 사람용 한 줄만).
- 현황: 라이브(rank-check 매 run 마지막 step, 비차단 — 실패해도 본체 영향 0).
- 확인법: cron 시각(00/06/12/18시+약 1시간) 직후 텔레그램 "상노체크 ..." 한 줄 수신.

### [이슈 #1 운영로그 댓글 — scripts/post_summary_to_issue.py]
⚠️Claude 정리·사장님 컨펌 대기(2026-07-18)
- 목적: cycle_summary 상세(개발자용 운영 로그)를 GitHub issue #1 댓글로 공개 기록. 멘션 없음=이메일 미발생(알림은 텔레그램으로 일원화).
- 현황: 라이브(매 run, permissions issues:write + github.token).
- 확인법: repo issue #1 최신 댓글 타임스탬프가 최근 cron과 일치하는지.

### [1등실패 색상표기 + 회색 전적·만료 — src/sheets.py]
⚠️Claude 정리·사장님 컨펌 대기(2026-07-18)
- 목적: 카페 구좌 1등 실패 '재발행 횟수'를 세서 키워드 셀 배경을 강도색(연노랑~빨강)으로 — 어디에 재발행 힘을 쓸지 시트만 봐도 보이게. 규칙: 판정은 발행+2일 후 / 1위 한 번이라도 달성한 키워드는 영구 색칠 면제 / 전적 있으나 현재 1등이면 옅은 회색, 3일 연속 1등 유지 시 회색 해제(CAFE_HISTORY_CLEAR_DAYS=3, 2026-07-18 14일→3일 단축, 커밋 4359675).
- 현황: 라이브(rank-check 본체 내, 매 cron 카운터 계산+색칠. raw_카페1등실패횟수 등 raw 컬럼에 기록).
- 확인법: 시트 키워드 셀 배경색 변화 + raw_카페1등실패횟수 컬럼 값.

### [type_preview 유형 C열 자동 write — src/type_preview.py]
⚠️Claude 정리·사장님 컨펌 대기(2026-07-18)
- 목적: 키워드의 최상단 구좌(AB/스마트블록/인기글)를 판정해 C열(유형)을 자동 갱신. bulk-guard(1회 100건 초과 or 50%+ 변경이면 hold)로 대량 오변경 차단.
- 현황: 라이브 자동(schedule run에서 TYPE_PREVIEW_WRITE_CONFIRMED=true — 2026-07-14 라이브 전환. src/main.py 상단 docstring "컨펌 전 write 금지"는 옛 문구로 workflows env가 실제 기준). 제안/감사 artifact도 매 run 생성.
- 확인법: 시트 C열이 cron 후 갱신 + run artifact `diagnostics-*` 안 type-previews.

### [stale 보호·재검사필요 — src/stale_preview.py + STALE_OUTPUT_FORMULA_MODE]
⚠️Claude 정리·사장님 컨펌 대기(2026-07-18)
- 목적: 키워드/링크(입력)가 바뀌었는데 옛 결과(K/L/M/O)가 그대로 보이는 사고 방지 — 입력 지문(input-key)이 다르면 "재검사필요" 표시.
- 현황: 라이브(schedule run에서 formula mode 기본 ON). dispatch 입력 recheck_stale_only로 재검사필요 행만 골라 돌리기 가능.
- 확인법: 키워드나 링크를 바꾼 행이 다음 cron 전 "재검사필요"로 뜨는지.

### [상위노출_이력 일별 아카이빙 — src/archive.py]
⚠️Claude 정리·사장님 컨펌 대기(2026-07-18)
- 목적: 매 cron이 읽은 노출 상태를 비공개 구글시트 '상위노출_이력' 탭에 키워드×일별 스냅샷으로 적재(날짜별 멱등 — 하루 4번 돌아도 1벌) → "언제 올랐다 빠졌나" 시계열 추적. 공개 repo라 데이터는 시트에만.
- 현황: 라이브(코드 기본은 OFF지만 rank-check.yml이 ARCHIVE_ENABLED='1' 고정 — 2026-07-02 사장님 "켜"). exposure_history.py가 이 탭으로 추세/코호트 분석(QA봇 답변용).
- 확인법: run 로그 "[아카이브] N행" + 비공개시트 상위노출_이력 탭 오늘 날짜 행.

### [sheet-backup artifact + 복원 — rank-check.yml + scripts/restore_backup.py]
⚠️Claude 정리·사장님 컨펌 대기(2026-07-18)
- 목적: 매 cron 시작 시 시트 전체 스냅샷(.json.gz)을 artifact로 업로드 — 사고 시 다운로드+restore_backup.py로 즉시 복원. QA봇·주간 다이제스트의 데이터 원천이기도.
- 현황: 라이브(매 run, 보존 14일 — free tier 용량 계산으로 30→14일 단축).
- 확인법: run 페이지 Artifacts에 `sheet-backup-<run_id>` 존재.

### [diagnostics artifact — rank-check.yml (traces/audits/previews)]
⚠️Claude 정리·사장님 컨펌 대기(2026-07-18)
- 목적: 행 단위 trace + invariant/post-write 감사 JSONL — 사장님 "이 행 왜 이래?" 질문 시 grep으로 원인 즉시 추적(D-032).
- 현황: 라이브(매 run, 보존 14일).
- 확인법: run 페이지 Artifacts에 `diagnostics-<run_id>` 존재.

### [카외 보관함 폴더보장 브리지 — rank-check.yml curl(ensureAllFolders)]
⚠️Claude 정리·사장님 컨펌 대기(2026-07-18)
- 목적: team-project 쪽 일1회 cron이 실제 0회 발동해 보관함 폴더 누락 누적(바디워시 70개 사건) → 확실히 도는 이 cron이 매번 Apps Script(ensureAllFolders, 멱등)를 호출해 폴더를 보장(2026-06-26 근본해결).
- 현황: 라이브(매 run, 비차단 — 순위체크 본작업 영향 0). 실체는 team-project 쪽 Apps Script, 여기는 호출만.
- 확인법: run 로그 "Ensure 카외 보관함 folders" step 성공 + 보관함 폴더 누락 경보 부재.

---

## B. OFF (자동 발송/실행 꺼짐 — 수동 dispatch만, 켜기는 사장님 명시 go 필수)

### [exposure-trend 상위노출 감소 알림 — exposure-trend.yml + scripts/exposure_trend_alert.py]
⚠️Claude 정리·사장님 컨펌 대기(2026-07-18)
- 목적: 상위노출_이력에서 주 평균 노출 건수를 비교해 10%+ 감소 시 텔레그램 경보(읽기 전용·기본 dry-run).
- 현황: OFF(2026-07-16 사장님 지시 — '전체 N개'가 노출 수 아닌 전체 키워드 수를 세던 문제. cron 주석 처리, 커밋 87993b9). 집계 수정 후 재가동 여부 사장님 결정 대기.
- 확인법: exposure-trend.yml schedule 블록이 주석인지 + `gh run list`에 schedule발 run 없음.

### [weekly-digest 주간 총괄 — weekly-digest.yml + scripts/send_weekly_digest.py]
⚠️Claude 정리·사장님 컨펌 대기(2026-07-18)
- 목적: sheet-backup 재가공으로 주 1회 총괄 다이제스트 텔레그램 발송(총괄자용).
- 현황: OFF(2026-06-23 — 마진봇 통합 주간보고로 대체, 중복 방지. cron 주석 처리). dispatch dry-run 점검만 가능.
- 확인법: weekly-digest.yml schedule 블록이 주석인지.

### [cafe-material-collect 재료 자동수집 — cafe-material-collect.yml + src/integration_runner.py(+jisikin/review/cafe_collect)]
⚠️Claude 정리·사장님 컨펌 대기(2026-07-18)
- 목적: 카외 제품 탭의 태깅 키워드로 지식인/리뷰 재료를 자동 수집해 수집결과_* 스테이징 탭 적재.
- 현황: OFF(D-062 2026-06-21 — 가동 게이트(태깅·API 키) 열기 전 cron 중지, '수집 0' 알림이 상노 채널 침범 방지. cron 주석 처리). 키 없으면 채널별 자동 스킵.
- 확인법: cafe-material-collect.yml schedule 블록이 주석인지.

---

## C. 수동 전용 (원래부터 cron 없음 — 필요할 때 dispatch)

### [auto-accuracy 파서 정확도 자동측정 — auto-accuracy.yml + scripts/auto_accuracy.py]
⚠️Claude 정리·사장님 컨펌 대기(2026-07-18)
- 목적: 100 키워드 랜덤 샘플로 parser vs ground truth 비교 — 파서 자체 결함 자동 발견.
- 현황: 수동 dispatch 전용. 결과는 artifact + issue #1 댓글.
- 확인법: Actions에서 auto-accuracy 수동 실행 → issue #1 "auto-accuracy 결과: NN%" 댓글.

### [cafe-apply-classify 키워드 분류 일괄반영 — cafe-apply-classify.yml + src/apply_keyword_classify.py]
⚠️Claude 정리·사장님 컨펌 대기(2026-07-18)
- 목적: CSV의 키워드 단계 분류('3 증상' 등)를 라이브 시트 '키워드 분류' 칸에 일괄 기록.
- 현황: 수동 dispatch 전용(no-auto-activation 규칙). dry_run='true'로 매칭 확인 후 실반영 권장.
- 확인법: Actions run 로그 + 대상 탭 '키워드 분류' 칸.

### [cafe-jisikin-smoketest 네이버 키 작동확인 — cafe-jisikin-smoketest.yml]
⚠️Claude 정리·사장님 컨펌 대기(2026-07-18)
- 목적: 네이버 Open API 키가 실제 작동하는지 지식인 fetch 1회로 즉시 확인(수집 파이프라인과 무관하게 키만 검증).
- 현황: 수동 dispatch 전용.
- 확인법: 수동 실행 → 로그 "키워드 'X' → 지식인 N건 수집 성공".

---

## D. 잔존 코드 (이 repo에서 부르는 워크플로 0개 — 손대지 말 것)

### [리포트·대시보드 코드 잔존분 — src/report_*.py, src/slack_notify.py, src/metric_guards.py, scripts/send_daily·weekly·monthly_dashboard.py, dashboard_fetch.py, send_telegram_report.py]
⚠️Claude 정리·사장님 컨펌 대기(2026-07-18)
- 목적: (과거) 일/주/월 이미지 리포트 + 한수연 슬랙 DM + 숫자 타당성 가드. 라이브 정본은 team-project `cafe-external/reports/`(cafe-dashboard.yml)로 이관됨.
- 현황: 죽음/이관 완료 — 이 repo의 cafe-dashboard·telegram-report(+watchdog) 워크플로는 07월 삭제(커밋 77986db·7099d40·8819f0d). 현재 워크플로 9개 중 이 스크립트들을 참조하는 곳 0개(grep 실측). ⚠️src/report_html.py·report_metrics.py에 커밋 안 된 로컬 수정 있음(용도 미확인).
- 확인법: `git grep dashboard .github/workflows/` 0건 + 리포트 작업은 무조건 team-project에서.

### [.harness 운영 문서 — 요청_원장.md(목적 앵커)·decisions.md·tasks.md 등]
⚠️Claude 정리·사장님 컨펌 대기(2026-07-18)
- 목적: 요청 드롭 방지 원장 + 목적 앵커(사장님 컨펌 2026-07-18) + 의사결정/작업 기록 — Claude 세션 간 이어달리기 기반.
- 현황: 수동(Claude가 작업 시 갱신). 요청_원장.md 앵커 커밋 cffb499.
- 확인법: `.harness/요청_원장.md` OPEN 항목 대조.

---

## 누락 가능성 (확인 못 한 것 — 정직 표기)

- **외부 cron-job.org 백업 트리거**: rank-check.yml 주석에 "외부 cron-job.org API 호출용"·"8회/일+외부 cron-job.org 백업"이라 적혀 있으나, cron-job.org 계정 실태(등록 여부·주기·살았는지)는 이 조사에서 접근 불가 — 미확인.
- **텔레그램 실수신**: 알림 발송 코드·run 성공은 확인했지만 사장님 폰에 실제 도착하는지는 실측 못 함(T-M12.7 QA봇 실수신 확인도 원장상 pending).
- **GitHub Secrets 실값**: SPREADSHEET_ID·SERVICE_ACCOUNT_JSON 등 존재는 최근 run 성공으로 간접 확인만(직접 조회 안 함). 옛 ACTIONS_PAT 삭제 여부도 미확인(원장상 사장님 go 대기).
- **이 PC의 로컬 예약작업(schtasks)**: 이 repo 관련 로컬 스케줄 존재 여부 미조사(조사 범위=repo 내부).
- **src/report_html.py·report_metrics.py 로컬 미커밋 수정분**: 누가 왜 고쳤는지(동기화 잔여물 vs 작업 중) 미확인.
- **tests/·.claude/·.omc/ 내부**: 존재만 확인, 상세 미조사.
- **issue #1 댓글 실게시**: 코드·권한은 확인, 최신 댓글 실물은 웹에서 미확인.
