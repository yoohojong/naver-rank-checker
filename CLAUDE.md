# CLAUDE.md — naver-rank-checker 프로젝트

## 새 세션 진입 시 즉시 자동 실행 (이 파일이 있으면 Claude가 자동 로드)

**1단계 — 컨텍스트 복원**:
1. `.harness/README.md` 읽기 — 프로젝트 한눈에
2. `.harness/tasks.md` 읽기 — 진척도 + 다음 작업
3. `.harness/PROTOCOL.md` 읽기 — 멀티 Claude 협업 규칙
4. 필요하면 `.harness/spec.md`, `.harness/plan.md`, `.harness/decisions.md` 추가 로드

**2단계 — 다음 task 자동 잡기**:
1. `tasks.md`에서 status=pending + deps 모두 completed인 task 찾기
2. `mkdir .harness/claims/T-{id}/` 시도 → 성공하면 claim
3. info.txt 작성 (session_id, claimed_at)
4. `plan.md`의 해당 task 상세 읽고 실행 (TDD 패턴)
5. 완료 후: tasks.md 갱신 → claims 폴더 삭제 → (git initialized면) commit

**3단계 — 사용자에게 보고**:
"M{번호}.{번호} 완료. 다음 ready: T-{id}. 진행할까요?"

## 사장님이 다음 세션에서 사용할 마법의 문장

이 중 하나만 입력하면 자동 이어감:

```
다음 task 진행
```

또는 더 명시적:

```
naver-rank-checker .harness 읽고 다음 task 이어가
```

또는 여러 task 자동 진행:

```
다음 ready task 전부 자동 진행. 사장님 작업 task 만나면 멈추고 가이드 출력.
```

## 프로젝트 한 줄 요약

네이버 키워드 상위노출 자동 체크 → Google Sheets 자동 갱신. GitHub Actions 6시간 cron, 무료 운영.

## 핵심 위치

- 코드: `src/` (10개 모듈)
- 테스트: `tests/unit/`, `tests/component/`
- 하네스: `.harness/`
- claims: `.harness/claims/T-{id}/` (mkdir 원자적 lock)

## 사용자 (사장님) 정보

- 비개발자, 코드 직접 수정 X
- 외주본 33만원 (프로그램88) 안정성 부족 → 자체 제작 결정
- 구조: 분리 + 효율 + 단호한 결정 선호 (취향 polling 금지)

## 🚨 영구 한국어 표준어 강제 (2026-05-14 사장님 평생 약속 명시 강화)

**사장님 페르소나 정합 한국어 표준어 사용 의무 — 평생 영구 적용**:

- 절대 금지 단어: `박`, `박은`, `박을`, `박음`, `박는`, `박지`, `박혀`, `박혔`, `박힘`, `박힌`, `박히`, `박았`, `박혀있`
- 절대 금지 줄임 표현: `박...`, `박..` (어근 회피 시도 = 단어 본체 노출 = 잘못된 회피 = 절대 X)
- 대체 동사 (자연 한국어 완성 문장 의무):
  - 한다 / 했다 / 합니다 / 하는 / 한 / 함
  - 적용한다 / 적용한 / 적용했다
  - 추가한다 / 추가했다 / 추가한
  - 실행한다 / 진행한다 / 사용한다 / 수정한다 / 호출한다
  - 있다 / 들어있다 / 존재한다 / 적용되어 있다
- 적용 범위: Claude 응답 출력 + 모든 commit message + 모든 문서 (CLAUDE.md / tasks.md / decisions.md / retro-log.md)
- 자체 검증 의무: 응답 작성 후 = 단어 "박" 검색 → 0회 확인 → 송신
- 근거: 2026-05-12 + 2026-05-14 사장님 평생 약속 강화 의무 — "평생 영원히 저급한 단어 X" 명시.

**사장님 지적 후 메타 학습 의무**:

사장님이 코드 / 동작 / 결과 지적할 때 = 단순 코드 fix X. 다음 단계 의무:
1. 진솔 사과 (변명 X)
2. root cause 분석 (Iron Law: Phase 1)
3. 영구 룰 추가 (CLAUDE.md + decisions.md + retro-log)
4. 재발 방지 메커니즘 (hook / Skill / 자동 검증)
5. 그 후 코드 fix

**1차 작업 후 사장님 의도 정합 검증 의무**:

사장님 모호 메시지 ("OR" / "이거 하던가 저거 하던가") = 우리가 옵션 선택 후 → **sample 또는 1차 결과 보여드린 후 사장님 confirm 받음 의무**. 832 행 같은 큰 범위 자동 적용 X.

근거: T-M10 / T-M13 두 번 사장님 의도 미스 = 우리 추측 = 시트 손상. T-M14 (사장님 1줄 confirm 후 진행) = D-018 룰 검증 완료.

---

## ⚠️ T-M13 학습 (2026-05-12) — 영구 룰

**사장님 의도 반영 시 — 모호 시점 = 우리 단호 결정 금지**:

1. **"OR" 시그널** = 사장님 메시지에 "이거 하던가 저거 하던가" / "둘 다 가능" / "상관없어" 있을 때:
   - 우리 단호 결정하지 않는다 (사장님 페르소나 = 단호하지만 **모호 의도 시점은 예외**).
   - **사장님 1줄 결정 받음 의무** ("ㅇ" / "B" / "잠깐").
2. **사장님 시트 컨벤션 변경 (마케터 작업 흐름 / 새 동작 / 스키마 영향)** 적용 시:
   - 1차 = sample 제출 또는 사장님 OK 받음.
   - 832 행 자동 적용 X.
3. **비즈니스 컨텍스트 (마케터 시점)**:
   - 작업자 빈 + 링크 빈 row = **마케팅 예정 (사장님 기획 단계)** = 처리 제외.
   - 작업자 있는 + 링크 있는 row = **진짜 작업 row** = 처리 진행.
   - K=AB 있어도 = 사장님 시트의 "C 컬럼 (사장님 의도 기록)" 존재 = 우리가 갱신하는 K 와 분리.

**근거**: 2026-05-12 T-M10 적용한 게 사장님 의도 정합 X. 사장님 시트 832 행 link 빈 row 에 잘못된 K/L/M 입력됨. T-M13 revert + 사장님 시트 정리 진행. (`.harness/decisions.md` D-018)
